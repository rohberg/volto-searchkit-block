import React, { useState } from 'react';

const FacetedSearch = ({ data }) => {
  // const [activeIndex, setActiveIndex] = useState(new Set());

  return <div>I am a Faceted Search component</div>;
};

export default FacetedSearch;
